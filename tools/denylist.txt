# Identifiers that must never appear anywhere under method/, at any position,
# inside a code fence, inside a code span or inside a translated sample.
#
# Unlike allowlist.txt, this list has no exemptions. It exists for the case
# where a person adapts the method itself and their own employer, product or
# client name slips into a file meant to be candidate-agnostic.
#
# One per line. Empty is a valid state and is how this template ships.
