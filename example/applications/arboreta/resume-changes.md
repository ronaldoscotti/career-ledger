# What changed against the master

The auditable difference between `profile/masters/resume.md` and `applications/arboreta/resume.md`, one line per decision, as `method/funnel/materials.md` requires.

It lives in its own file rather than at the top of the resume. `tools/build/md2resume.py` renders every line of the markdown it is given, including a markdown comment, so a change block inside the resume arrives in the sent file as a paragraph of notes about the resume.

Derived on 2026-08-26 for Arboreta, Senior Data Platform Engineer.

- Tagline names ingestion and late-arriving data, in the posting's own words. The posting uses "late-arriving data" three times and the master never used the phrase.
- Ingestion promoted out of the Lomvik bullet list into its own section, because the posting spends its first two paragraphs and five of the nine responsibilities there.
- The watermark decision is now the longest item on the page. It is the closest thing she has to the posting's central problem and it was the fourth bullet in the master.
- Trakvia cut from two bullets to one. Nothing in it serves this posting.
- Stack order rewritten to the posting's order, and their spelling kept. They write "Airflow" without "Apache" and they write "Postgres".
- The warehouse she has not used in production is not mentioned and not excused. The gap stays on the card and the answer is prepared in the dossier.
- The 2021 gap kept, in the same words as the master. Shortening it makes it look handled.
